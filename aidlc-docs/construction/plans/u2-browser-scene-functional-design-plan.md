# U2 浏览器体验与 3D 场景：功能设计计划

## 设计目标

为 U2 定义技术无关但面向玩家的 React/R3F 交互与显示行为。U2 只能将按钮、平台命中和帧时钟转换为 U1 的白名单动作；不得复制/修改 U1 规则。设计范围包含 HUD、R3F 场景、小羊/木板视觉、音效、错误降级、页面可见性和资源清理。

## 执行清单

- [x] 读取 U2 组件、服务、故事映射和已实现的 U1 公开接口。
- [x] 确认可选木板提示、通关过渡、初始音效和 WebGL 失败体验。
- [x] 建立 `domain-entities.md`，定义 U2 ViewModel、会话快照、场景表现状态和 UI 事件。
- [x] 建立 `business-logic-model.md`，定义会话调度、R3F 输入、动画、音效、可见性和重玩/卸载流程。
- [x] 建立 `business-rules.md`，定义 UI 验证、可访问性、错误降级、资源清理和领域边界规则。
- [x] 建立 `frontend-components.md`，定义 React/R3F 树、props/状态、交互和 `data-testid` 规范。
- [x] 验证 U2 覆盖 US-01 至 US-07 的 UI/场景职责，并保持对 U1 的单向依赖。

## 需要确认的交互决策

请在每个 `[Answer]:` 后填写一个选项字母；若选择 X，请在字母后补充具体偏好。

## 问题 1：可选木板提示

当木板可作为下一跳目标时，应如何提示玩家？

A) 使用金色边缘/轻微发光和悬停抬升；不可选木板保持正常材质，且状态文本说明“发光木板可跳”（推荐）

B) 仅在鼠标悬停时改变可选木板颜色

C) 所有木板同样显示，由玩家自行判断

X) 其他（请在下方 `[Answer]:` 后说明）

[Answer]: A

## 问题 2：通关过渡

抵达对岸后、下一关自动开始前，应显示多长的视觉反馈？

A) 显示约 1 秒“过关！”提示与庆祝效果，然后自动进入下一关（推荐）

B) 立即切换下一关，不显示过渡

C) 显示提示并等待玩家点击“下一关”

X) 其他（请在下方 `[Answer]:` 后说明）

[Answer]: A

## 问题 3：初始音效状态

首次打开游戏时音效开关应默认如何设置？

A) 默认开启；仅在玩家点击“开始游戏”后初始化 Web Audio，浏览器拒绝时无声继续（推荐）

B) 默认静音，玩家自行开启

C) 没有静音开关，始终尝试播放

X) 其他（请在下方 `[Answer]:` 后说明）

[Answer]: A

## 问题 4：WebGL 不可用时的体验

浏览器不支持 WebGL 或 R3F 场景初始化失败时，应提供何种降级？

A) 显示安全的覆盖提示，说明需要支持 WebGL 的现代浏览器，并提供“重新加载”按钮；不尝试运行 2D 替代游戏（推荐）

B) 显示纯 CSS/DOM 的简化 2D 木板游戏

C) 仅显示空白区域与控制台错误

X) 其他（请在下方 `[Answer]:` 后说明）

[Answer]: A

## 审批

完成答案后，请明确批准本 U2 功能设计计划。获批后会生成 U2 领域实体、逻辑模型、业务规则和前端组件文档。

[Plan Approval]: Approve
